import { RouteComponentProps } from '@reach/router';
import Layout from './Layout';

const SignupPage: React.FC<RouteComponentProps> = () => (
  <Layout title="Signup">
    <div className="mt-8 text-gray-600">Not for now, sorry.</div>
  </Layout>
);

export default SignupPage;
