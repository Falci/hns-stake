{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "hns-stake",
  "logging": false,
  "entities": ["src/models/**/*.ts"],
  "migrations": ["src/db/migrations/**/*.ts"],
  "cli": {
    "migrationsDir": "src/db/migrations"
  }
}
